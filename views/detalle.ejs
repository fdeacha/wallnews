<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= noticia.titulo %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      background: #000; /* Fondo negro */
      color: white; /* Texto blanco */
    }
    .detalle {
      max-width: 800px;
      margin: auto;
      background: #222; /* Fondo oscuro */
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
    }
    .detalle img {
      width: 100%;
      height: auto;
      border-radius: 8px;
    }
    .detalle h1 {
      margin-top: 20px;
      color: white;
    }
    .detalle p {
      font-size: 16px;
      color: #ccc;
      line-height: 1.6;
      margin-top: 10px;
    }
    .volver {
      display: block;
      text-align: center;
      margin-top: 20px;
      text-decoration: none;
      background: #007bff;
      color: white;
      padding: 10px 20px;
      border-radius: 8px;
      font-weight: bold;
    }
    .volver:hover {
      background: #0056b3;
    }
  </style>  
</head>
<body>
  <div class="detalle">
    <img src="<%= noticia.imagen || 'https://via.placeholder.com/800x400' %>" alt="Imagen de la noticia">
    <h1><%= noticia.titulo %></h1>
    <p><%= noticia.contenido %></p>
    <a href="/" class="volver">Volver al inicio</a>
  </div>
</body>
</html>
